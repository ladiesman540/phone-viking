{
  "name": "phone-viking",
  "version": "0.1.0",
  "private": true,
  "description": "Dispatch dashboard and Millis function server for phone-based service routing.",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  }
}
